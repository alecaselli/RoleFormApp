<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.EditActivity"></activity>
        <activity android:name=".ui.BagActivity" />
        <activity android:name=".ui.CreateNewItem" />
        <activity android:name=".ui.CreateNewSpell" />
        <activity android:name=".ui.EquipActivity" />
        <activity android:name=".ui.StatsActivity" />
        <activity android:name=".ui.CurrencyActivity" />
        <activity android:name=".ui.CreateNewCharacterActivity" />
        <activity android:name=".ui.CreditsActivity" />
        <activity android:name=".ui.InfoActivity" />
        <activity android:name=".ui.NoteActivity" />
        <activity android:name=".ui.AddNewItem" />
        <activity android:name=".ui.AddNewSpell" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.CharacterActivity" />
    </application>

</manifest>